## [1.1.7] — 2026-02-19

### Исправлено
- **Сборка** — версия в `manifest.json` расширения теперь подставляется из `package.json` при сборке; в zip-артефакте релиза версия в манифесте совпадает с версией релиза.
