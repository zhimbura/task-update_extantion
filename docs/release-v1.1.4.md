## [1.1.4] — 2026-02-19

### Исправлено
- **Парсинг списка задач** — разделителем строк ввода считается только перевод строки (`\n`); запятые и точки с запятой больше не разбивают строку на части (раньше использовалось `split(/[\n,;]+/)`).
